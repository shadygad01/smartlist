# تقرير فحص هيكل مشروع Smartlist

**تاريخ الفحص:** 15 أغسطس 2026
**الفرع:** `main`
**آخر commit مفحوص:** `bcafbeb11`
**نطاق الفحص:** الهيكل، نقاط التشغيل، الاعتماديات، الاختبارات، سير عمل CI/CD، الاتساق بين الوثائق والتنفيذ، ومخاطر التشغيل والأمان.
**حالة المستودع أثناء الفحص:** نظيفة، ولم تُجرَ أي تعديلات وظيفية.

## 1. الملخص التنفيذي

المشروع ليس تطبيقًا صغيرًا ذا نقطة دخول واحدة، بل منظومة متعددة الطبقات تجمع بين **محرك Python للتحليل والمسح والإشعارات**، و**واجهة Vite/React/TypeScript**، و**قواعد بيانات SQLite وملفات JSON/CSV مولّدة**، إضافة إلى عشرة مسارات GitHub Actions للنشر والتحديث والاختبارات. النسخة الحالية تحتوي على عدد كبير من الملفات: 228 ملف Python، و140 ملف TSX، و10 workflows، فضلًا عن مستندات وبيانات تشغيلية كثيرة.

الهيكل غني وظيفيًا، لكنه يعاني من مؤشرات واضحة على **انجراف معماري وتراكم انتقالات بين أجيال مختلفة من النظام**. توجد تناقضات مباشرة بين الاختبارات والواجهة الحالية، وخطأ صياغة يمنع تجميع ملف بحثي، ومسارات تشغيل تسمح بفشل بعض الفحوصات دون إيقاف النشر، واعتماديات Python غير مثبتة بإصدارات دقيقة. لذلك أوصي بالتعامل مع الإصلاحات على مرحلتين: أولًا تثبيت بوابات البناء والنشر ومنع الفشل الصامت، ثم إعادة تنظيم الحدود بين الإنتاج والبحث والبيانات المولدة.

> **التقدير العام:** المشروع قابل للاستمرار، لكن موثوقية النشر الحالية أقل من مستوى التعقيد الوظيفي الذي يقدمه. أعلى المخاطر ليست في نقص الميزات، بل في أن ينجح المسار الآلي بينما تكون بعض ضمانات الاتساق أو القياسات البحثية غير مفعّلة فعليًا.

## 2. خريطة الهيكل الحالي

| الطبقة | المكونات الرئيسية | الملاحظة المعمارية |
|---|---|---|
| التشغيل الإنتاجي | `main.py`، `signal_engine.py`، `discount_reversal_engine.py`، `signal_db.py`، `notifications/` | منطق إنتاجي كبير موزع بين ملفات جذرية كثيرة، مع وجود طبقة orchestrator أحدث |
| التنسيق والجدولة | `notifications/scan_orchestrator.py`، `scheduler.py`، workflows | يوجد مساران للتشغيل؛ المسار الرسمي هو orchestrator، بينما `scheduler.py` يبدو أقدم وأكثر اعتمادًا على ملفات/جداول غير مضمونة |
| البحث والتعلم | `research_engine.py`، `continuous_learning.py`، `labs/`، `optimization_engine.py`، `rule_discovery.py` | ينبغي عزله بوضوح عن مسار الإنتاج ومنع أي ترقية غير موثقة إلى قواعد القرار |
| البيانات والحالة | SQLite، JSON، CSV، فرع `data`، ملفات snapshot | توجد مصادر متعددة للحقيقة، مع حاجة إلى سياسة أوضح للملكية، الإصدار، وإعادة البناء |
| الواجهة | `artifacts/egx-commandcenter`، React/TypeScript، Vite، SnapshotProvider | الاتجاه المعماري قائم على snapshot واحد، لكن صفحة Dashboard تخالف عقد الاختبارات الحالي |
| الجودة والنشر | `audit/`، `tests/`، `.github/workflows/` | توجد فحوصات كثيرة، لكن بعضها مسموح له بالفشل أو لا يُشغّل في workflow النشر |

الوثيقة `PROJECT_MAP.md` مفيدة كخريطة مفاهيمية، لكنها لا تعكس الحالة الحالية بالكامل؛ فهي تذكر أعدادًا وبنى أقدم من الواقع الحالي، كما تشير إلى ملفات غير موجودة في النسخة المفحوصة. هذا يجعلها مرجعًا وصفيًا لا مصدرًا موثوقًا للبنية الفعلية.[1]

## 3. نتائج الفحص المؤكدة

### 3.1 خطأ صياغة يمنع تجميع `rule_discovery.py`

فشل `python3 -m compileall` عند الملف `rule_discovery.py` في السطر 22، لأن العبارة `from __future__ import annotations` جاءت بعد إسناد المتغير `RESEARCH_ONLY_LAB = True`. يجب أن تأتي تعليمات `__future__` بعد docstring الوحدة مباشرة وقبل أي إسناد أو استيراد فعلي.[2]

**الأثر:** أي مسار يستورد هذا الملف أو يشغله مباشرة سيفشل قبل تنفيذ منطق اكتشاف القواعد.
**الإصلاح المقترح:** نقل `from __future__ import annotations` إلى السطر التالي مباشرة بعد docstring، ثم إعادة تشغيل التجميع والاختبارات. هذه إصلاحية عالية الأولوية ومنخفضة المخاطر.

### 3.2 انجراف مباشر بين عقد اختبار الواجهة والتنفيذ

الاختبار `tests/test_dashboard_architecture.py` يفرض ألا تستورد صفحة Dashboard مكوّن `ValuationSection`، لأن التقييم العادل يجب أن يظهر داخل بطاقات الإشارة فقط. التنفيذ الحالي في `artifacts/egx-commandcenter/src/pages/DashboardPage.tsx` يستورد المكوّن ويعرضه داخل شبكة البحث.[3] [4]

**الأثر:** الاختبار المعماري سيفشل عند تشغيله، كما أن الواجهة قد تعرض طبقة تقييم مستقلة خلافًا للعقد التصميمي المعلن.
**الإصلاح المقترح:** اختيار أحد مسارين بقرار معماري صريح:

1. إزالة استيراد وعرض `ValuationSection` من `DashboardPage.tsx` والإبقاء على `ValuationPanel` داخل بطاقات الشراء وإعادة التجميع، وهو المسار المتوافق مع الاختبار الحالي.
2. أو تعديل عقد الاختبار والوثائق إذا كان العرض المستقل مقصودًا فعليًا، مع تحديث جميع الضوابط المرتبطة بذلك. لا أوصي بالمسار الثاني قبل توثيق سبب التغيير.

### 3.3 بوابات اتساق مهمة تسمح بالفشل وتستمر في النشر

في `dashboard_refresh.yml`، فحصا `assert_price_consistency.py` و`assert_event_timeline_consistency.py` مضبوطان على `continue-on-error: true`. كما يحتوي `full_production_scan.yml` على عدة خطوات فاشلة مسموح لها بالاستمرار.[5] [6]

**الأثر:** قد يُنشر dashboard أو snapshot رغم وجود اختلاف بين السعر المستخدم في القرار والسعر المعروض، أو رغم عدم تطابق timeline مع إشارات الشراء. هذا أخطر من فشل البناء، لأن الفشل يصبح غير مرئي ويصل إلى المستخدم النهائي.

**الإصلاح المقترح:** تقسيم الفحوصات إلى نوعين. فحوصات سلامة القرار والاتساق والسعر يجب أن تكون **حاجزة blocking**، بينما تقارير البحث الاختيارية يمكن أن تبقى non-blocking مع تسجيل واضح. ينبغي أيضًا إخراج ملخص فشل موحد كـ artifact، بدل الاكتفاء بـ `continue-on-error`.

### 3.4 مسار الجدولة القديم يخفي غياب قياس النتائج

` scheduler.py` يستورد `bottom_quality` داخل `_measure_outcomes()`، لكنه يتعامل مع `ImportError` بصمت ويعيد صفرًا. الملف `bottom_quality.py` غير موجود في النسخة الحالية.[7]

**الأثر:** يمكن تشغيل دورة التعلم وكأنها اكتملت بينما لم تُقَس نتائج الإشارات أصلًا. قيمة `outcomes_measured = 0` لا تُعامل كفشل في `run_once()`، كما أن الوثيقة تصف دورة scan → measure → learning دون ضمان أن مرحلة القياس نفذت.

**الإصلاح المقترح:** بما أن `notifications/scan_orchestrator.py` يعلن نفسه كسلطة التنفيذ الوحيدة، ينبغي إما إزالة `scheduler.py` القديم أو تحويله إلى wrapper واضح يستدعي orchestrator. وإذا كان قياس النتائج مطلوبًا، فيجب اعتماد وحدة موجودة ومختبرة له، مع جعل غيابها فشلًا صريحًا في بيئة الإنتاج لا `pass` صامتة.

### 3.5 الوثائق ومسارات التشغيل لا تزال تشير إلى ملفات غير موجودة

README يوجّه المستخدم إلى `QUICK_START.txt` و`شرح_النظام_الجديد.txt`، لكن الملفين غير موجودين في الجذر. كذلك تشير `PROJECT_MAP.md` إلى عدة ملفات لا توجد في checkout الحالي، من بينها `challenger_scanner.py` و`snapshot_engine.py` و`feature_registry.py` و`outcome_enricher.py` و`backtest.py` و`backtest_analysis.py`.[1] [8]

**الأثر:** صعوبة onboarding، وصعوبة تمييز ما هو legacy من ما هو إنتاجي، واحتمال أن تكون بعض خرائط الاعتماديات غير قابلة للتنفيذ.

**الإصلاح المقترح:** إنشاء فحص CI بسيط يتحقق من الروابط والملفات التي تذكرها الوثائق، ثم تحديث README وPROJECT_MAP من manifest آلي. ينبغي وسم الملفات البحثية والقديمة بوضوح بدل إبقاء أسماء تاريخية في الخريطة الحالية.

### 3.6 الاعتماديات Python غير قابلة لإعادة الإنتاج بالكامل

`requirements.txt` يستخدم حدودًا دنيا فقط، مثل `pandas>=1.5.0` و`scikit-learn>=1.3.0` و`lightgbm>=4.0.0`. في المقابل، تعتمد workflows على `pip install -r requirements.txt` مباشرة. بعض الاعتماديات الكبيرة غير مثبتة في بيئة الفحص الحالية، منها `yfinance` و`apscheduler` و`xgboost` و`lightgbm` و`shap` و`imodels` و`mlxtend`.

**الأثر:** نفس commit قد يعمل بصورة مختلفة بين تشغيل محلي وGitHub Actions، وقد تتغير نتائج النماذج أو تتعطل imports بسبب تحديث إصدار غير متوقع.

**الإصلاح المقترح:** اعتماد ملف lock أو constraints مثبت، مثل `requirements.lock.txt` مولد في بيئة Python 3.11، مع hashes حيثما أمكن. يجب فصل اعتماديات الإنتاج الأساسية عن اعتماديات البحث الثقيلة، حتى لا يصبح تشغيل المسح اليومي مرهونًا بكل مكتبات ML الاختيارية.

### 3.7 تغطية الواجهة لا تمنع الانحراف الحالي

حزمة الواجهة الحالية تحتوي على scripts للبناء والتحقق النوعي فقط، ولا تحتوي على script للاختبارات أو lint. كما أن workflow الخاص بتحديث لوحة التحكم يبني الحزمة ولا يشغّل اختبار `test_dashboard_architecture.py`.[5] [9]

**الأثر:** يمكن أن يمر build TypeScript بينما يخالف التنفيذ عقدًا معماريًا مهمًا. وجود الاختبار في المستودع دون ربطه ببوابة النشر يجعله توثيقًا غير مُنفّذ.

**الإصلاح المقترح:** إضافة `test:architecture` إلى scripts أو إلى workflow مخصص، وتشغيله قبل النشر. كما أوصي بإضافة lint وdependency audit و smoke test لقراءة `presentation_snapshot.json` من build الناتج.

### 3.8 مخاطر تسريب بيانات الإشعارات والـ artifacts

`main.py` يضع عنوان البريد المستهدف في قيمة ثابتة داخل الشيفرة، بينما `notifications/email_sender.py` يحفظ نسخة كاملة من HTML في `runtime_email.html` قبل الإرسال، ويشغّل SMTP debug logging الذي يطبع حوار SMTP إلى السجل.[10] [11]

**الأثر:** وجود عنوان شخصي داخل كود عام أو قابل للنشر، واحتمال الاحتفاظ ببيانات المحافظ والأسعار داخل artifact محلي، واحتمال تسجيل تفاصيل SMTP غير اللازمة. حتى مع STARTTLS لا ينبغي تسجيل حوار بروتوكول البريد في الإنتاج.

**الإصلاح المقترح:** نقل عنوان المستلم إلى `REPORT_EMAIL_TO` أو قائمة recipients في secret/config غير متعقب، وتعطيل debug logging افتراضيًا. إذا كان حفظ HTML مطلوبًا للتدقيق، فيجب وضعه في مسار artifacts مضبوط الصلاحيات مع سياسة احتفاظ قصيرة، وعدم حفظه في checkout أو artifact عام.

## 4. ترتيب الأولويات

| الأولوية | الإجراء | السبب | نطاق التغيير | معيار القبول |
|---|---|---|---|---|
| P0 | إصلاح ترتيب `__future__` في `rule_discovery.py` | يمنع التجميع المباشر | ملف واحد | `compileall` يمر دون SyntaxError |
| P0 | جعل price/event/decision consistency gates حاجزة | يمنع نشر snapshot غير متسق | workflows | أي فشل في gate يفشل job والنشر |
| P0 | حسم `ValuationSection` مقابل عقد الاختبار | يعالج انجرافًا مؤكدًا | الواجهة + الاختبار/الوثيقة | architecture test وbuild يمران |
| P1 | إزالة أو إعادة تعريف `scheduler.py` القديم | يمنع نجاح دورة تعلم بلا قياس | orchestrator/scheduler | لا يوجد `ImportError` صامت لمرحلة أساسية |
| P1 | تثبيت اعتماديات Python وفصل optional ML | يحسن قابلية إعادة الإنتاج | requirements/CI | بيئة نظيفة تعيد نفس lock ويعمل smoke test |
| P1 | تشغيل الاختبارات المعمارية في CI | يمنع عودة الانحراف | workflows/package scripts | PR يفشل عند مخالفة العقد |
| P1 | إزالة العنوان الثابت وSMTP debug وحفظ HTML غير المنضبط | تحسين أمني وتشغيلي | main/email_sender/CI | لا توجد بيانات شخصية ثابتة ولا SMTP dialog في logs |
| P2 | تحديث README وPROJECT_MAP آليًا | تقليل انجراف الوثائق | docs/script CI | كل رابط ملف في الوثائق صالح أو موسوم legacy |
| P2 | توحيد ملكية البيانات بين SQLite/JSON/data branch | تقليل تضارب الحالة | data policy/scripts | كل artifact له مصدر حقيقة وسياسة إعادة بناء |

## 5. خطة تنفيذ مقترحة

### المرحلة الأولى: تثبيت الجودة ومنع الفشل الصامت

ابدأ بإصلاح خطأ `rule_discovery.py`، ثم أضف smoke test يستورد نقاط الدخول الرئيسية دون إجراء اتصالات خارجية. بعد ذلك عدّل workflows بحيث تفشل عند فشل فحوصات السعر والقرار والتسلسل الزمني. يجب أن يكون نجاح النشر مشروطًا بنجاح هذه البوابات، وليس بمجرد نجاح Vite build.

### المرحلة الثانية: تثبيت العقد المعمارية

احسم وضع `ValuationSection` وفق القرار التصميمي، ثم اربط `tests/test_dashboard_architecture.py` بعملية CI. أضف اختبارًا يثبت أن كل مكوّن يستخدم `SnapshotProvider` بدل fetch مستقل، واختبارًا يقرأ snapshot المبني من `dist` للتأكد من أن مسار GitHub Pages الفعلي يعمل.

### المرحلة الثالثة: تنظيف حدود التشغيل

اجعل `notifications/scan_orchestrator.py` نقطة الدخول الوحيدة، وقلّل `scheduler.py` إلى طبقة توافق أو انقله إلى archive بعد إثبات عدم استخدامه. يجب أن تُرجع كل مرحلة من دورة التشغيل status typed واضحًا يميز بين `success` و`skipped` و`degraded` و`failed`، وألا يُعتبر غياب قياس النتائج نجاحًا.

### المرحلة الرابعة: إعادة إنتاج البيئة والبيانات

أنشئ lockfile Python أو constraints مثبتًا، وافصل dependencies الخاصة بالبحث عن runtime الإنتاجي. ثم وثّق مصدر الحقيقة لكل من SQLite وJSON وCSV وفرع `data`، مع أوامر إعادة بناء قابلة للتنفيذ من checkout نظيف.

## 6. ما لم يتم الجزم به

لم أعتبر كل `pass` في المشروع خطأً؛ كثير منها قد يكون fallback مقصودًا. كما لم أعدّل الملفات أو أنشئ بيئة Node/Python جديدة لتثبيت جميع الاعتماديات، ولذلك لم يُنفّذ build فعلي للواجهة في هذه الجولة. نتيجة التجميع الوحيدة المؤكدة هي وجود SyntaxError في `rule_discovery.py`، بينما تعذر تشغيل pytest محليًا لأن pytest غير مثبت في البيئة الحالية. ينبغي إعادة تشغيل الاختبارات بعد تثبيت بيئة dependencies المقفلة.

## 7. الخلاصة

أفضل تحسين للمشروع ليس إضافة محرك جديد، بل **تقليل عدد مسارات الحقيقة والتنفيذ**. الأولوية العملية هي إصلاح SyntaxError، منع بوابات الاتساق من الفشل الصامت، ربط اختبار الواجهة بـ CI، ثم توحيد orchestrator والاعتماديات. بعد ذلك فقط يصبح من المنطقي تقييم جودة النماذج والبحث، لأن نتائج البحث لا يمكن الوثوق بها تشغيليًا إذا كانت مرحلة القياس أو snapshot أو النشر قابلة للتجاوز دون فشل صريح.

## المراجع

[1]: PROJECT_MAP.md — خريطة المشروع والملفات المعلنة.
[2]: rule_discovery.py:19-22 — موضع `RESEARCH_ONLY_LAB` وتعليمة `__future__`.
[3]: tests/test_dashboard_architecture.py:32-45 — عقد منع `ValuationSection` في صفحة Dashboard.
[4]: artifacts/egx-commandcenter/src/pages/DashboardPage.tsx:2-46 — الاستيراد والعرض الفعلي لـ `ValuationSection`.
[5]: .github/workflows/dashboard_refresh.yml:89-128 — فحوصات الاتساق وبناء الواجهة.
[6]: .github/workflows/full_production_scan.yml:460-589 — خطوات non-blocking في المسار الإنتاجي.
[7]: scheduler.py:80-92 — استيراد `bottom_quality` والتعامل الصامت مع غيابه.
[8]: README.md:9-24 — روابط التشغيل والتوثيق غير الموجودة في checkout.
[9]: artifacts/egx-commandcenter/package.json:6-10 — scripts الحالية للحزمة.
[10]: main.py:132 — عنوان البريد الثابت.
[11]: notifications/email_sender.py:57-99 — حفظ HTML وSMTP debug logging.
