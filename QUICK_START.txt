QUICK START - 4 FILES ONLY

════════════════════════════════════════════════════

FILES TO DOWNLOAD:

1. workflow.yml
2. main.py  
3. requirements.txt
4. .gitignore

════════════════════════════════════════════════════

COPY TO YOUR REPO:

workflow.yml → .github/workflows/daily_scan.yml
main.py → main.py
requirements.txt → requirements.txt
.gitignore → .gitignore

════════════════════════════════════════════════════

ADD GITHUB SECRETS:

EMAIL_USER = shady.gad@live.com
EMAIL_PASS = [Gmail App Password]

Settings → Secrets and variables → Actions

════════════════════════════════════════════════════

PUSH:

git add .
git commit -m "update: three-mode email system"
git push origin main

════════════════════════════════════════════════════

TEST:

In GitHub Actions:
- Click "Run workflow" (Manual)
- Wait 2-3 minutes
- Check email ✓

════════════════════════════════════════════════════

DONE!

Tomorrow 8:30 AM → Email ✓
Every 5 min (9-2:30 PM) → Email (if BUY) ⚠️
Manual button → Email now 🔧

════════════════════════════════════════════════════

IMPORTANT:

Add your SMC logic to scan_stocks() in main.py!
The code has placeholders you must replace.

════════════════════════════════════════════════════
