# 🔍 دليل استكشاف الأخطاء - لماذا لا تظهر المراكز المفتوحة؟

## الوضع الحالي:

أنا اختبرت الكود في بيئتي وتحققت من أن:
- ✅ الملف `open_positions.json` موجود فيه 10 صفقات مفتوحة
- ✅ الدالة `build_report()` تبني الإيميل بشكل صحيح
- ✅ القسم "المراكز المفتوحة" معروض في الإيميل
- ✅ جميع 10 صفقات تظهر مع الأسعار والأهداف الديناميكية

**النتيجة: الكود يعمل بشكل صحيح في بيئة الاختبار.**

لكنك تقول أنت **لا تراها في بريدك الفعلي**. هذا يعني المشكلة في **بيئتك أنت** وليس في الكود.

---

## 🎯 الأسباب المحتملة (بالترتيب):

### 1️⃣ **open_positions.json فارغ في بيئتك** (الاحتمال الأعلى)
البرنامج في بيئتك قد لم ينشئ أي صفقات مفتوحة بعد.

**للتحقق:**
```bash
# في مجلد المشروع لديك
cat open_positions.json
```

هل الملف فارغ أو به `{}`؟

---

### 2️⃣ **GitHub Actions لم يشغل الكود الجديد بعد**
قد تكون GitHub Actions تشغل نسخة قديمة من الكود.

**للتحقق:**
```bash
# تحقق من آخر تشغيل لـ GitHub Actions
# اذهب إلى: https://github.com/shadygad01/smartlist/actions
```

هل آخر تشغيل بعد الساعة 8:30 صباحاً؟

---

### 3️⃣ **الأسعار الحالية غير متوفرة**
قد تكون API لا تعود بيانات أسعار، فالكود يستخدم الأسعار المحفوظة في `current_price`.

**للتحقق:**
- هل المشكلة حدثت أول مرة اليوم؟
- أم أنها قديمة منذ فترة؟

---

## ✅ خطوات الحل:

### الخطوة 1: تحقق من البيانات لديك
```bash
# 1. اذهب لمجلد المشروع
cd /path/to/smartlist

# 2. تحقق من الملف
ls -lh open_positions.json

# 3. شوف محتواه
cat open_positions.json | python3 -m json.tool | head -50
```

**ماذا تتوقع أن ترى:**
- حجم الملف > 100 بايت
- محتوى يحتوي على أكثر من صفقة واحدة
- كل صفقة فيها `"status": "open"`

---

### الخطوة 2: اختبر الكود يدويًا
```bash
cd /path/to/smartlist
python3 -c "
import sys
sys.path.insert(0, '.')
from main import daily_scan
daily_scan()
"
```

**ماذا تتوقع أن ترى:**
```
📅 Daily scan started at...
  Fetching Dow Jones status...
  Analyzing: Commercial International Bank ...
  ...
✅ Daily scan completed!
```

---

### الخطوة 3: تحقق من محتوى الإيميل المتوقع
```bash
cd /path/to/smartlist
python3 -c "
import sys
sys.path.insert(0, '.')
from main import build_report

html, _ = build_report()

# تحقق من وجود 'المراكز المفتوحة'
if 'المراكز المفتوحة' in html:
    print('✓ قسم المراكز المفتوحة موجود')
    # احسب عدد الصفقات
    count = html.count('.CA')
    print(f'✓ عدد الصفقات: {count}')
else:
    print('✗ قسم المراكز المفتوحة غير موجود!')
"
```

---

## 🚀 الحل النهائي:

إذا اتضح أن `open_positions.json` فارغ في بيئتك، فأنت تحتاج إلى:

### خيار 1: نسخ البيانات الاختبارية
```bash
cd /path/to/smartlist

# قم بتشغيل السكريبت الذي ينشئ بيانات اختبارية
python3 initialize_positions.py
```

### خيار 2: التقويس على GitHub Actions
تأكد من أن GitHub Actions يعمل في الأوقات الصحيحة:
- 8:30 AM صباحاً (Daily Scan)
- 10:00 AM - 2:30 PM (Continuous Scan)

---

## 📧 بعد تشغيل الكود:

الإيميل الذي ستستقبله **يجب أن يحتوي على:**

```
📊 المراكز المفتوحة — التارجت الديناميكي

| السهم | سعر الدخول | السعر الحالي | التارجت الديناميكي | مستوى Fib | تاريخ الدخول |
|-------|----------|-----------|--------------|--------|-----------|
| COMI.CA | 38.20 EGP | 52.67 EGP (+37.9%) | 57.30 EGP | 50% | ... |
| TMGH.CA | 12.80 EGP | 21.07 EGP (+64.6%) | 25.60 EGP | 100% | ... |
| ... | ... | ... | ... | ... | ... |
```

---

## ❓ أسئلة للمساعدة:

1. ما محتوى `open_positions.json` لديك الآن؟
2. متى كان آخر بريد استقبلته من النظام؟
3. تم تعديل الملف متى آخر مرة؟

أرسل لي الإجابات وسأساعدك!

